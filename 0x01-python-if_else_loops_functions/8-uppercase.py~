#!/bin/python3
def uppercase(str):
    str2 = ""
    for i in str:
        if (ord(i) >= 97 and ord(i) <= 123):
            i = ord(i) - 32
            print(f"{i:c}", end="")
        else:
             print(f"{i}", end="")
